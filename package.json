{
  "name": "angle-wars",
  "version": "2.0.0",
  "description": "Angle Wars — a twin-stick arena shooter (Phaser 3 + Vite).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "cap:sync": "cap sync",
    "cap:copy": "cap copy",
    "assets:mobile": "bash scripts/gen-mobile-assets.sh"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/haptics": "^8",
    "@capacitor/ios": "^8.4.2",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^6.0.7",
    "vitest": "^3.0.0"
  }
}
